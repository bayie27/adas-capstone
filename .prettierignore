pnpm-lock.yaml
uv.lock
dist/
node_modules/
.venv/
ai_engine/snapshots/
ai_engine/sample_vids/

# Frozen artifact staged from the research repo `adas_detection`. It is the
# reference the ported detector is diffed against to prove the port changed
# no behaviour (SPEC.md §8 step 3), so it must stay a faithful copy — never
# reformat it.
ai_engine/adas_transfer/

# Nested git worktrees (.claude/worktrees/<name>) are separate checkouts on
# other branches; prettier scans the raw filesystem regardless of branch, so
# without this it fails on whatever those in-progress branches currently
# contain.
.claude/worktrees/

# Gitignored agent working files (per-task briefs, reports and the progress
# ledger). Machine-generated and never committed, but prettier scans the raw
# filesystem, so without this `pnpm check` fails on ~40 files nobody edits by
# hand. Same reasoning as the worktrees entry above and as Ruff's blanket
# Markdown exclusion in pyproject.toml.
.superpowers/

# Disposable pytest base-temp directories. These can contain generated reports
# and inherited worktree files, neither of which is repository source.
.pytest-tmp-*/

# Untracked team working docs — never touch these
*.pdf
final_paper_text.txt
be_masterplan_text.txt
*_review.md
*_decisions.md
ai_engine_detection_pipeline_handoff.md
backend_assessment.md
backup_restore_explained.md
dx_ci_tooling_plan.md
be_plan/
be_audit/

# Generated by paper_sync/build_tracker.py — regenerating it would fail format:check
# on every run, since Prettier re-pads Markdown table columns.
paper_sync/TRACKER.md
