# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# Database
adas.db
adas.db-journal
adas.db-wal
adas.db-shm
app.db
app.db-journal
app.db-wal
app.db-shm

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
# This shared lifecycle module is source code, not generated library output.
!scripts/lib/
!scripts/lib/adas-lifecycle.psm1
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#pdm.lock
#pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Abstra
# Abstra is an AI-powered process automation framework.
# Ignore directories containing user credentials, local state, and settings.
# Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file. However, if you prefer, 
#  you could uncomment the following to ignore the entire vscode folder
# .vscode/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore

# Local agent/skill metadata. .agents/skills/ is the shared skill directory
# Codex and Antigravity both read, so it is tracked; everything else here is local.
.agents/*
!.agents/skills/
skills-lock.json
.uv-cache/

# Manually exported copy of the defense document, for runtimes with no Drive access.
paper_sync/.local/

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Media
*.mp4
*.avi
*.mkv
*.jpg
*.jpeg
*.png
# Help Center article screenshots are intentional, tracked content, not
# incidental media.
!frontend/public/help/*.png

# Frontend
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Testing
/coverage

/dist
/dist-ssr
/build

.vite
.eslintcache
*.tsbuildinfo

.DS_Store
Thumbs.db


.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# Playwright
test-results/
playwright-report/
# ...but the visual baselines are a committed artefact, and the global *.png
# rule above would otherwise swallow them (FE_Implementation.md §6.5).
!e2e/visual.spec.ts-snapshots/*.png
.playwright/

# Local artifacts from retired capacity implementations. Nothing reads these.
ai_engine/capacity_report.json
ai_engine/machine_profile.json
ai_engine/_bench/

# Disposable pytest base-temp directories used for isolated test artifacts.
.pytest-tmp-*/

ai_engine/*.engine
# Build artifacts, not sources: both are exported from epoch50.pt, which IS
# tracked. The .engine is tied to the GPU, driver and TensorRT version that
# built it and is useless anywhere else; the .onnx is an intermediate of the
# same export chain. Rebuild them per machine rather than committing 10-20 MB
# of binary that no other machine can load.
ai_engine/*.onnx

# Real accident footage, test-only, permanently. No public licence; shows identifiable
# people/vehicles/locations. Already covered by *.mp4 above; this is belt-and-suspenders
# so the rule holds even if a non-.mp4 file ever lands here.
ai_engine/eval/clips/

# MediaMTX generates a self-signed cert and EC private key for its MoQ/WebRTC
# listeners on first run, in whatever directory it is started from. The
# documented command is `mediamtx mediamtx.yml` from the repo root, so every
# developer who simulates cameras drops these here. Never commit a .key.
auto.crt
auto.key

# Kept local, deliberately not published. This repository is public.
#   Final-Paper.pdf         — the capstone paper itself, a 3.2MB binary
#   paper-edits-required.md — corrections the port makes necessary to that paper
# port-handover.md IS tracked: be_plan/ and be_audit/ establish that internal
# planning and audit documents belong in this repo, and it is what lets a fresh
# session resume the work.
ai_engine/docs/Final-Paper.pdf
ai_engine/docs/paper-edits-required.md

# TLS certs (LAN demo profile) — never commit a private key
certs/

# Optional real frames for seeded snapshots (dev_plan/01_PKG_seed_core.md Step 6).
# Absence is fine - the seeder falls back to an embedded placeholder JPEG.
backend/app/dev/assets/snapshots/

# Claude Code: share skills with the team, keep everything else local.
# .claude/worktrees/ holds full repo checkouts and must never be committed.
.claude/*
!.claude/skills/

# Local isolated agent worktrees.
.worktrees/
.logs/
