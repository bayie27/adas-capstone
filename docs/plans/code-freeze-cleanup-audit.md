# Code-freeze cleanup — disposition ledger

Execution record for `docs/plans/code-freeze-cleanup.md`. Historical/non-executable once cleanup is declared complete; it documents what happened, not new instructions.

## Baseline

- Baseline `HEAD`: `041c863e99ea0a4c005897739a1c674d8998cd36` (`main`, tag-free). Revalidated at the start of this session: `git rev-parse HEAD` on `chore/code-freeze-cleanup` still reports `041c863` — no new baseline commits landed underneath the branch.
- Branch: `chore/code-freeze-cleanup`, checked out at repo root.
- Baseline tracked-file count: 658, matching the plan's recorded inventory.
- At session start the working tree already carried substantial uncommitted progress from a prior session (65 deletions, 77 modifications, 60 new untracked files — see full breakdown below). Nothing has been staged or committed; all changes described here are working-tree state, reviewed and continued by this session.
- No push, PR, deploy, reseed, or live Google Drive write occurred. All commands were local and read-only against Drive/paper artifacts.

## Executors

Lead (sole editor/mutator, this session) + two read-only subagents spawned within this session:

- **Code/tests auditor** — reviewed runtime code, test relevance, references, dependencies, deletion-candidate evidence. Reported PASS with two concerns (both resolved by the lead — see "Findings resolved" below).
- **Docs/artifacts auditor** — reviewed documentation, links, historical evidence, local artifacts, worktrees. See "Docs/artifacts auditor findings" (appended after completion).

Both ran with read-only tool access; no edits, package installs, or test runs came from them.

## Required moves — status: all landed at exact target paths

| Source (baseline)                                                                   | Destination                                                  | Verified                                                        |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------------ | --------------------------------------------------------------- |
| `LAN_SETUP.md`                                                                      | `docs/operations/LAN_SETUP.md`                               | Content moved; root/CLAUDE.md/CONTRIBUTING.md links repointed   |
| `be_plan/*` (26 files)                                                              | `docs/archive/be_plan/*`                                     | 1:1 filename match, all 26 present at destination               |
| `be_audit/*` (11 files)                                                             | `docs/archive/be_audit/*`                                    | 1:1 filename match, all 11 present at destination               |
| `dev_plan/*` (6 files)                                                              | `docs/archive/dev_plan/*`                                    | 1:1 filename match, all 6 present at destination                |
| `be_decisions_review.md`                                                            | `docs/archive/be_decisions_review.md`                        | Present                                                         |
| `paper-audit.md`                                                                    | `docs/archive/paper-audit.md`                                | Present; `paper_sync/PROCEDURE.md` repointed                    |
| `LAN_DEMO_HANDOFF.md`                                                               | `docs/archive/validation/LAN_DEMO_HANDOFF.md`                | Present, same filename                                          |
| `NFR04_LAN_MEASUREMENT_HANDOFF.md`                                                  | `docs/archive/validation/NFR04_LAN_MEASUREMENT_HANDOFF.md`   | Present, same filename                                          |
| `UAT_READINESS_DRY_RUN_HANDOFF.md`                                                  | `docs/archive/validation/UAT_READINESS_DRY_RUN_HANDOFF.md`   | Present, same filename                                          |
| `UAT_READINESS_DRY_RUN_RERUN_LOG.md`                                                | `docs/archive/validation/UAT_READINESS_DRY_RUN_RERUN_LOG.md` | Present, same filename                                          |
| `test-execution-validation-plan.md`                                                 | `docs/validation/test-execution-validation-plan.md`          | Present                                                         |
| `unit-testing-coverage-review.md`                                                   | `docs/validation/unit-testing-coverage-review.md`            | Present                                                         |
| `frontend/VISUAL_CHECKLIST.md`                                                      | unchanged (kept alongside frontend code)                     | Confirmed in place, referenced from `docs/validation/README.md` |
| `paper_sync/`, `.agents/skills/adas-paper-sync/`, `.claude/skills/adas-paper-sync/` | unchanged (kept in place)                                    | Confirmed in place                                              |

All archived documents carry a "historical/non-executable" framing at their new index pages (`docs/archive/README.md`, `docs/architecture/README.md`'s "Historical rationale" section) without asserting resolution of open findings. No `OPEN_ITEMS.md` was created anywhere.

## New documentation scaffolding created

| File                                     | Purpose                                                                                                                            |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `docs/README.md`                         | Navigation/authority guide across all doc areas                                                                                    |
| `docs/architecture/README.md`            | Current architecture and contracts, extracted from `be_plan`/`be_decisions_review` before archiving, cross-referencing D-001…D-012 |
| `docs/architecture/repository-layout.md` | Annotated module/entrypoint map                                                                                                    |
| `docs/operations/README.md`              | Consolidated setup/environment/launcher/dev-panel/maintenance guide                                                                |
| `docs/validation/README.md`              | Test commands and links to dated validation references                                                                             |
| `docs/archive/README.md`                 | Historical-records index with non-authoritative framing                                                                            |
| `ai_engine/docs/README.md`               | AI-engine documentation index (component-local, per target layout)                                                                 |

**Fix applied this session:** `docs/architecture/README.md` had mojibake from a prior encoding mishap — `â†’` in place of `→` (incident lifecycle line) and `â€“` in place of `–` (the D-001…D-012 range) — consistent with this repo's known Windows/cp1252 write-path gotcha. Repo-wide grep for the same corruption pattern across `.md/.py/.ts/.tsx/.toml/.ps1` found no other occurrences. Fixed in place; both arrows/en-dash now render correctly.

## Confirmed deletions

| Item                                                                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scratch/patch_{adduser,ai,audit,audit2,changepass,confirmdelete,edituser,users}.py` (8 files) | One-off frontend-source-rewriting scripts, never executed as part of this cleanup, no CI/doc/script references anywhere in the tracked tree. `scratch/` is now empty.                                                                                                                                                                                                                                                           |
| `frontend/src/hooks/useDeliveryBacklog.ts` + `.test.ts`                                        | Hook always returned `null`; banner could never appear. Zero remaining references to `DeliveryBacklog`/`deliveryBacklog`/`delivery-backlog` anywhere in `frontend/src/` (code-auditor grep, corroborated independently).                                                                                                                                                                                                        |
| `frontend/src/components/DeliveryBacklogNotice.tsx` + `.test.tsx`                              | Same feature; import/invocation/comment removed cleanly from `App.tsx`, mocks removed cleanly from `App.test.tsx`.                                                                                                                                                                                                                                                                                                              |
| `frontend/pnpm-lock.yaml`                                                                      | Nested lockfile; root `pnpm-workspace.yaml` is the workspace owner (`packages: [frontend]`); root `pnpm-lock.yaml` is byte-identical to baseline (`git diff 041c863 -- pnpm-lock.yaml` empty) — no dependency-version drift introduced.                                                                                                                                                                                         |
| `ai_engine/epoch50-32.engine` (local, gitignored, 8,979,574 bytes)                             | Batch-32 diagnostic build artifact. Repo-wide grep confirms nothing references it; `AI_MODEL_PATH` default/example and `ai_engine/README.md` both still point at `epoch50.engine`. Historical batch-comparison measurements preserved untouched in `ai_engine/docs/AI_ENGINE_PIPELINE_OPTIMIZATION_PLAN_REVIEW.md` and `var/log/engine-comparison/old32-{a,b}.json` (gitignored evidence, left in place). Deleted this session. |

Preserved, explicitly not deleted: `ai_engine/epoch50.engine`, `ai_engine/epoch50.onnx`, tracked `ai_engine/epoch50.pt`.

## AI outbox integrity (delivery-backlog removal boundary)

`ai_engine/outbox.py`, `ai_engine/supervisor.py`, `ai_engine/backend_client.py`, `ai_engine/tests/test_supervisor.py` each show only a single-line comment path update (`be_audit/...` → `docs/archive/be_audit/...`). Storage, retry, and quarantine logic is unchanged — confirmed independently by the code auditor. This satisfies the plan's explicit requirement to preserve the working AI outbox while removing the dormant frontend consumer of it.

## Modified files — comment/path-only (verified, grouped)

The code auditor read every diff hunk in the following files in full and confirmed each is exclusively (a) a stale-comment/implementation-phase-narration removal that touches no logic, or (b) a path update following a documentation move — no control flow, signature, runtime import, test assertion, fixture, or config default changed in any of them:

- **Backend routes/services/core/dev/schemas/maintenance:** `backend/app/services/incidents.py`, `backend/app/core/config.py`, `backend/app/core/security.py`, `backend/app/main.py` (plus one "restates the code" comment removed — the bulk-UPDATE aside — with the substantive self-blindfold-ordering comment preserved), `backend/app/dev/{assets,profiles,seed,service,wipe}.py`, `backend/app/api/routes/{analytics,cameras,dev,internal}.py`, `backend/app/schemas/{events,internal}.py`, `backend/app/maintenance/cli.py`.
- **Backend tests:** `backend/tests/test_internal.py` (7 path-only edits across race/boundary/hostile-input test classes — bodies/assertions untouched), `backend/tests/{perf/conftest.py, perf/test_export_performance.py, perf/test_query_performance.py, test_alerts.py, test_app_factory.py, test_audit.py, test_auth.py, test_cameras.py, test_dev_seed.py, test_dev_tools.py, test_exports.py, test_maintenance_schedule.py, test_reports.py, test_users.py}`.
- **AI engine:** `ai_engine/backend_client.py`, `ai_engine/supervisor.py`, `ai_engine/tests/test_supervisor.py` (see outbox-integrity note above).
- **Frontend:** `frontend/src/hooks/{useNow,useOverlayBehavior,usePagination}.ts` (stale phase-narration collapsed to factual one-liners; state/logic unchanged), `frontend/src/pages/AuditLog.tsx`, `frontend/src/pages/detections/IncidentDetailModal.tsx` (same pattern — long phase-history narration replaced by a factual comment; props/JSX unchanged), `frontend/src/utils/apiFieldErrors.ts`, `frontend/vite.config.ts`, `frontend/src/api/dev.ts`, `frontend/src/components/ui/Modal.test.tsx`, `frontend/src/test/wrapper.tsx`.
- **Scripts/config:** `backend/scripts/{daily_restart.sh,offline_restore.sh,seed_dev_data.py}`, `scripts/{adas-maintenance.ps1,register-maintenance-task.ps1}`, `scripts/start-dev.ps1` (all edits are `LAN_SETUP.md` → `docs/operations/LAN_SETUP.md` substitutions in comments/help text/operator-facing strings — no flag parsing or bring-up ordering changed), `.env.example` (2 path-only comment edits; no variable name/default/`AI_MODEL_PATH` line touched), `.github/workflows/ci.yml` (1-line comment path update, no job/step/trigger logic touched).
- **`pyproject.toml`** (4 lines): 1 line is the approved placeholder-description fix (`"Add your description here"` → real description); the other corrects a stale comment count ("Two tiers are excluded" → "Three tiers", matching the `markers`/`addopts` list which already had 3 entries at baseline). `addopts`, `markers`, and the TensorRT `<11` pin with its four direct package declarations are all absent from the diff — untouched.
- **Root docs with link-path updates only:** `CLAUDE.md`, `CONTRIBUTING.md`, `README.md` (see redesign below), `.gitignore`/`.prettierignore` (see tooling section), `ai_engine/docs/{AI_ENGINE_LIVE_SESSION_REPORT.md,training_docs/*.md}`, `paper_sync/{CLAIM_SOURCES.md,PROCEDURE.md,findings/*.md}` — each diffed by the lead directly; all hunks are `be_plan/…`/`be_audit/…`/`dev_plan/…`/`LAN_SETUP.md`/`test-execution-validation-plan.md`/`UAT_READINESS_DRY_RUN_HANDOFF.md`/`be_decisions_review.md`/`paper-audit.md` path substitutions with no prose-meaning change.

**Correction (caught by the second-pass code auditor, see below):** `ai_engine/README.md`, `backend/README.md`, and `backend/scripts/README.md` were originally lumped into the bucket above. That was wrong — they received real content edits, not just path substitutions: `backend/README.md` reworded its `.env`/API-contracts/test-coverage prose and corrected the `test_internal.py` coverage description (v1 routes were already gone at baseline, so "webhook (v1/v2)" was stale); `backend/scripts/README.md` corrected the DB-location and schema-provisioning description and removed an alternate "run from the backend directory" invocation that contradicted `CLAUDE.md`'s repo-root convention; `ai_engine/README.md` is where the corrupted "Where to look next" table lived (see the fix above). All three edits were independently re-verified as factually accurate against current code (`init_db()` → `check_schema_revision()` does run Alembic; `DATABASE_URL` does default to `sqlite:///./adas.db`), and are squarely inside the plan's §3 instruction to "reconcile backend and AI READMEs with current code" — so the edits themselves are fine. Only this ledger's earlier characterization of them was inaccurate, now fixed.

## Placeholder metadata (plan §4)

- `pyproject.toml` description: fixed by the prior session (confirmed above).
- `frontend/package.json`: code auditor flagged that `"name": "frontend"` / `"version": "0.0.0"` had no `"description"` field, unlike the `pyproject.toml` sibling fix. **Resolved this session** — added `"description": "ADAS React dashboard for incident review, camera management, analytics and system operations."`, matching `frontend/README.md`'s own framing. `"name"`/`"version"` left as-is: `"frontend"` is the correct pnpm-workspace package name (not a placeholder), and `0.0.0` is normal for a private, unpublished workspace package.

## Tooling — `.gitignore` / `.prettierignore`

Both were simplified (boilerplate for unused ecosystems — Django, Flask, Scrapy, Sphinx, PyBuilder, Jupyter, pipenv/poetry/pdm/pixi, PyCharm/VSCode/Cursor template comments — stripped) while all active exclusion rules and asset exceptions were preserved. `.prettierignore` gained a `docs/archive/` exclusion (replacing the narrower `be_plan/`/`be_audit/` entries, consistent with "update formatter exclusions for moved archives") plus new local-cache/worktree exclusions (`.cache/`, `.codex-uv-cache/`, `.codex-pytest-tmp/`, `.uv-cache/`, `.worktrees/`, `var/`, `tmp/`, `playwright-report/`, `test-results/`).

**Fix applied this session:** the initial `.gitignore` simplification pass had also stripped the _rationale_ comments attached to several non-obvious, intentional exceptions (real-accident-footage privacy note on `ai_engine/eval/clips/`, the TLS-private-key note on `certs/`/`auto.key`, the "why `epoch50.onnx`/`.engine` are gitignored despite being derived from a tracked `.pt`" note, the `Final-Paper.pdf`/`paper-edits-required.md` kept-local-deliberately note, the `.claude/worktrees/` never-commit note, the Playwright visual-baseline exception's `FE_Implementation.md §6.5` citation, the retired-capacity-artifacts note, and the seed-snapshot fallback note). The _rules themselves_ were never at risk (all exceptions still functioned correctly, confirmed by `prettier --check`/build passing), but the missing rationale is exactly the kind of "why," not "what," explanation this repo's comment policy protects — losing it would make a future contributor more likely to delete an exception without understanding its legal/security/technical reason. Restored all of the above, updating stale internal cross-references (`be_plan/`/`be_audit/` → `docs/archive/be_plan/`/`docs/archive/be_audit/`) in the process. `.prettierignore`'s own rationale comments were intact throughout and needed no changes.

## Local artifacts

| Artifact                      | Tracked?         | Disposition                                                                                                                  |
| ----------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `ai_engine/epoch50-32.engine` | No (gitignored)  | Deleted this session (see above).                                                                                            |
| `ai_engine/epoch50.engine`    | No (gitignored)  | Preserved — active default build artifact.                                                                                   |
| `ai_engine/epoch50.onnx`      | No (gitignored)  | Preserved — export intermediate, per plan default.                                                                           |
| `ai_engine/epoch50.pt`        | Yes              | Preserved, untouched — the adopted checkpoint.                                                                               |
| `.codex-pytest-tmp/`          | N/A (gitignored) | Left untouched — inaccessible during planning per the handoff; not re-attempted destructively this session either. Deferred. |

**Remaining `var/`/`tmp/` inventory** — first pass (docs auditor + lead, read-only) deferred everything ambiguous rather than guess; a follow-up double-check pass asked the user directly about each ambiguous bucket and acted on the answers. Final disposition:

| Path                                                                                                                                                | Size                       | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `var/backups/`, `var/uat-runs/`, `var/uat-safety-backups/`, `var/p30-drill-*/`                                                                      | ~35 MB                     | **Protected** — explicit plan category. Untouched.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `var/log/engine-comparison/` (`old32-a.json`, `new10-a/b.json`)                                                                                     | small                      | **Protected** — the batch-32 comparison evidence the plan says to preserve. Untouched.                                                                                                                                                                                                                                                                                                                                                                 |
| `var/log/gpu-resident/` (2,437 files) + `var/log/{detector-overhead,fps-diagnosis,hardware-capacity,live-demo,pipeline-review,preprocess-threads}/` | ~652 MB combined           | One-off GPU/perf diagnostic scratch. **User asked directly; answer: keep.** Untouched.                                                                                                                                                                                                                                                                                                                                                                 |
| `var/log/{ai_engine,backend,restore-coordinator,maintenance}-*.log` and `*.transcript.log`                                                          | ~130 MB, hundreds of files | Routine per-run stdout/stderr/transcripts from `start-dev.ps1`/`adas-maintenance.ps1`, Aug 16 → Sep 11 (today). **User asked directly; answer: keep all of it.** Untouched.                                                                                                                                                                                                                                                                            |
| `var/exports/`                                                                                                                                      | was 328 MB, 1,860 files    | Generated CSV/PDF/ZIP exports, TTL-expiring by design (`EXPORT_DIR`). **User asked directly; answer: delete.** **Deleted this session** — directory recreated empty since the backend expects `EXPORT_DIR` to exist.                                                                                                                                                                                                                                   |
| `var/pytest-p27/`, `var/pytest-p27-escalated/`, `var/pytest-p30-clean-20260904-121237/`, `var/uv-cache-p27/`                                        | ~73 KB combined            | Pytest/uv-cache scratch leaked into `var/` from a prior manual test run — unambiguous, no evidence value. **Deleted this session** without a separate ask, given the triviality.                                                                                                                                                                                                                                                                       |
| `var/p4_ai_drill.db-shm`, `var/p4_ai_drill.db-wal`                                                                                                  | ~757 KB combined           | **New discovery, not in the original inventory**: orphaned SQLite WAL/SHM sidecar files (dated Aug 9) with **no corresponding `.db` file** anywhere in `var/`. Matches the plan's explicit "Databases and SQLite sidecars" protected category verbatim. **Deferred — not touched**, needs a human decision since this session cannot ask the plan whether an orphaned sidecar is safe to remove with the same confidence as the exports/scratch above. |
| `var/run/`, `var/tmp/`                                                                                                                              | small/empty                | Runtime state. Left alone.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tmp/pdfs/uat-signoff-qa*/`                                                                                                                         | 3.9 MB, 20 PNGs            | UAT sign-off PDF page-renders. Treated as **protected** (UAT evidence) despite living under `tmp/`.                                                                                                                                                                                                                                                                                                                                                    |
| `.codex-pytest-tmp/`                                                                                                                                | —                          | Still inaccessible under this session's permissions, same as at planning time. No regression, unresolved.                                                                                                                                                                                                                                                                                                                                              |
| `.cache/`                                                                                                                                           | —                          | This session's own execution-tracking files (`cleanup-baseline.json`, `cleanup-moves.json`, etc.), gitignored, not accidentally tracked. Informational only.                                                                                                                                                                                                                                                                                           |
| `be_plan/`, `be_audit/`, `dev_plan/` (top-level)                                                                                                    | 0 bytes                    | Empty shells left behind after every file inside was moved. **Removed this session** (`rmdir`, confirmed empty first — the user asked why these were still there).                                                                                                                                                                                                                                                                                     |
| `playwright-report/`, `test-results/`                                                                                                               | ~536 KB                    | Regenerable Playwright HTML report + last-run marker. **Deleted this session** — trivially regenerable, gitignored.                                                                                                                                                                                                                                                                                                                                    |
| `publisher.log`, `remux.log` (repo root)                                                                                                            | 0 bytes each               | Empty stray logs from an earlier MediaMTX/ffmpeg run, flagged by the second-pass docs auditor as missing from this ledger. **Deleted this session.**                                                                                                                                                                                                                                                                                                   |
| `.worktrees/` (top-level, distinct from `.claude/worktrees/`)                                                                                       | 0 bytes                    | Empty, gitignored, flagged by the second-pass docs auditor as missing from this ledger. **Deleted this session.**                                                                                                                                                                                                                                                                                                                                      |
| `.ruff_cache/`, `.uv-cache/`, `.codex-uv-cache/`, `.codex-pytest-tmp/`, `backend/.pytest_cache/`, `frontend/node_modules/.vite/`                    | ~16.2 MB combined          | Pure tool caches (Ruff, uv, pytest, Vite dev-server) — no possible evidence or config value, always transparently regenerated. **Deleted this session**, no ask needed; re-ran `ruff check .` immediately after to confirm it rebuilds its cache and still passes clean. `.codex-pytest-tmp/` had shown as inaccessible during the original planning pass but was actually empty and accessible by this session — resolved, not a regression.          |

## Worktrees

Plan required keeping worktrees outside the repo (`.codex/worktrees/*`) untouched, and explicitly required keeping `.claude/worktrees/edge-cases-audit-testing-182a81` (holds untracked `be_plan/19_PKG_fe_backend_gaps.md` and `be_plan/20_FRONTEND_HANDOFF.md`).

Independently reverified all five in-repo worktrees this session (`git -C <worktree> status`, `git merge-base --is-ancestor <head> main`):

| Worktree                          | HEAD merged into `main`?                     | Uncommitted content                                                                                                                                                                            | Disposition                                                          |
| --------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `be-p7-backup-ops-8e40ae`         | Yes (detached HEAD)                          | Untracked `AGENTS.md` only — diffed byte-for-byte against a pre-split draft of what is now `CLAUDE.md`/`AGENTS.md`; fully superseded, no unique content                                        | **Removed** (`git worktree remove --force`)                          |
| `be-p8-help-center-cc4188`        | Yes (detached HEAD)                          | Same stale `AGENTS.md` draft                                                                                                                                                                   | **Removed**                                                          |
| `demo-seeding-dev-tools-8a9df5`   | Yes (`claude/demo-seeding-dev-tools-8a9df5`) | Same stale `AGENTS.md` draft; `git status` also showed `M CLAUDE.md` but `git diff`/`git diff --raw` on it were both empty — CRLF-normalization display artifact, no actual content difference | **Removed**                                                          |
| `elastic-einstein-efad56`         | Yes (`claude/elastic-einstein-efad56`)       | None — fully clean, `AGENTS.md` identical to root                                                                                                                                              | **Removed**                                                          |
| `edge-cases-audit-testing-182a81` | Yes (`feat/be-p19-fe-backend-gaps`)          | Untracked `be_plan/19_PKG_fe_backend_gaps.md`, `be_plan/20_FRONTEND_HANDOFF.md`, plus the same stale `AGENTS.md` draft                                                                         | **Kept**, per explicit plan instruction — not relocated or discarded |

**New discovery, not in the plan's original 7-worktree count:** `.claude/worktrees/p4-ci-fix/` — a completely empty directory (0 entries, dated Aug 9, no `.git` pointer file — i.e. it was not even a registered git worktree at planning time). No content to lose. Disposition: disposable, same reasoning class as the other three removed worktrees.

**Deferred — Windows path-length limitation:** `git worktree remove --force` successfully unregistered `be-p7-backup-ops-8e40ae`, `be-p8-help-center-cc4188`, and `demo-seeding-dev-tools-8a9df5` from git's worktree metadata (confirmed: no `.git/worktrees/<name>` admin entry remains for any of them, and none appear in `git worktree list`), but the physical directory deletion failed partway with `Filename too long` (Windows `MAX_PATH`). They are **no longer git worktrees**.

**Correction (caught by the second-pass docs auditor, then independently confirmed by the lead with a direct `ls`):** this ledger originally described these as "full old checkouts... containing `node_modules/`, `pnpm-lock.yaml`, `uv.lock`, and a `var/` directory," at ~262–269 MB each. That overstated what's actually there. Direct inspection (`ls -la .claude/worktrees/be-p7-backup-ops-8e40ae`) confirms `git worktree remove --force` had in fact already deleted the entire tracked source tree — no `backend/`, `frontend/src`, `ai_engine/`, `docs/`, `AGENTS.md`, or `CLAUDE.md` remain in any of the three — before the recursive OS-level directory delete choked on `node_modules/`'s deep paths and aborted. What's actually left in each is only: `node_modules/` (gitignored, trivially reinstallable), `var/` (small — 7.1 MB / 622 KB / 88 KB, the same regenerated-test-backup content already assessed as low-risk below), and a handful of loose top-level tracked files (`README.md`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `pyproject.toml`, `uv.lock`, `playwright.config.ts`, `scripts/`) that are identical/superseded copies of what's already in the main checkout — no unique content of any kind. This makes deletion _more_ clearly safe than the original description implied, not less. (Two independent `du` passes measured these at 269/262/263 MB and 229.6/223.2/223.7 MB respectively — a ~15% discrepancy most likely from measurement method/timing rather than actual content change; either figure is in the right ballpark for planning purposes.)

This session's sandbox blocked both a PowerShell robocopy-mirror workaround and a plain `rmdir` on these specific paths (an auto-mode classifier restriction on `.claude/worktrees/` deletions, independent of git and independent of file content — it also blocked an empty, content-free `p4-ci-fix/`). **Action needed from a human with a normal terminal:** run `rmdir /s /q` (cmd) or a long-path-aware `Remove-Item -Recurse -Force` on:

- `.claude/worktrees/be-p7-backup-ops-8e40ae` (~230–269 MB, almost entirely `node_modules/`)
- `.claude/worktrees/be-p8-help-center-cc4188` (~223–262 MB, almost entirely `node_modules/`)
- `.claude/worktrees/demo-seeding-dev-tools-8a9df5` (~224–263 MB, almost entirely `node_modules/`)
- `.claude/worktrees/p4-ci-fix` (empty, 0 bytes)

These are disk cleanup only — git already considers all four gone, no tracked source remains in any of them, and nothing in the repository's behavior depends on them.

## Verification results

| Check                                                                                                                                                                                       | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm --filter frontend test:run`                                                                                                                                                           | **Pass** — 53 test files, 353 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `pnpm --filter frontend lint`                                                                                                                                                               | **Pass** — clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `pnpm --filter frontend typecheck`                                                                                                                                                          | **Pass** — clean                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `pnpm --filter frontend build`                                                                                                                                                              | **Pass** — production build succeeds; all lazy page chunks and `DevPanel` chunk still split correctly, confirming lazy-loaded pages and the runtime-gated dev panel survived untouched                                                                                                                                                                                                                                                                                                                                                                    |
| `uv run pytest -n auto` (full backend suite — run because `core/config.py`, `core/security.py`, and `main.py`/app-factory were all touched, triggering the cross-cutting full-suite policy) | **Pass** — 1214 passed, 1 skipped, 103 warnings (all pre-existing: argon2 deprecation notice, one SQLAlchemy reflection warning — unrelated to this cleanup)                                                                                                                                                                                                                                                                                                                                                                                              |
| `uv run ruff check .`                                                                                                                                                                       | **Pass** — all checks passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `uv run ruff format --check .`                                                                                                                                                              | **Pass** — 212 files already formatted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `pnpm exec prettier --check .`                                                                                                                                                              | **Pass** — all matched files use Prettier style (confirms `.prettierignore` archive-exclusion update didn't let moved history get silently reformatted, and didn't over-exclude anything that should be checked)                                                                                                                                                                                                                                                                                                                                          |
| `backend/tests/perf/*`                                                                                                                                                                      | Not run — marked `slow`, excluded from default collection by `addopts`; the touched perf files (`conftest.py`, `test_export_performance.py`, `test_query_performance.py`) carry only comment/path edits per the code auditor, so re-running the ~30s-to-seed slow suite would be a confirmation run against unchanged logic. Deferred to the pre-PR `pnpm full:check` gate.                                                                                                                                                                               |
| Link/anchor audit, Help-Center-slug check, frozen-`adas_transfer/`-untouched check, "No Alembic until P9" sweep                                                                             | Delegated to the docs/artifacts auditor — see its findings below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `backend/alembic/`, `e2e/` (Playwright specs + `visual.spec.ts-snapshots/`), `playwright.config.ts`, `deploy/systemd/`, `mediamtx.yml`                                                      | **Pass** — a fourth-pass code auditor flagged that the ledger never documented reviewing these plan-required areas (§2 "Required audit coverage"). Confirmed by the lead: `git diff 041c863 --stat` is empty for all five paths (nothing needed changing), and a targeted grep of each for `be_plan/`/`be_audit/`/`dev_plan/`/`LAN_SETUP.md`/etc. found zero stale references — so there was nothing to fix, but the coverage itself is now on record. All 9 Playwright visual baselines under `e2e/visual.spec.ts-snapshots/` are present and untouched. |
| `pnpm check` / `pnpm full:check`                                                                                                                                                            | Not run — per policy, these are lifecycle gates for push/PR time, not per-change gates; no push or PR was authorized this session                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Second full-suite re-run (frontend tests/lint/typecheck, full backend pytest)                                                                                                               | **Pass, unchanged** — re-ran during a follow-up double-check pass: frontend 53/53 files, 353/353 tests; backend 1214 passed, 1 skipped, same as the first run. No drift.                                                                                                                                                                                                                                                                                                                                                                                  |

## Findings resolved this session

From the **code/tests auditor**:

1. `frontend/package.json` placeholder metadata — **fixed** (description added).
2. `ai_engine/epoch50-32.engine` still on disk — **fixed** (deleted).

From the **lead's own direct review** (not flagged by either subagent, found while spot-checking the diff):

3. `docs/architecture/README.md` mojibake (`â†’`, `â€“`) — **fixed**.
4. `.gitignore` rationale-comment loss on intentional exceptions — **fixed** (restored, with paths updated to match the new archive layout).
5. `ai_engine/README.md`'s "Where to look next" table was corrupted: a prior edit updating the "How many cameras will this machine carry?" row to "How do I estimate inference capacity?" had spilled four rows from the "Module map" table above it (`frames.py`/`gpu_camera.py`/`gpu_preprocess.py`/an orphaned `capacity.py` line) into the middle of this table, breaking its column structure entirely. **Fixed** — reconstructed the 5-row table (`Why is it shaped this way?` / `What measurement closed this constant?` / `How do I re-measure detection quality?` / `How do I estimate inference capacity?` → `capacity.py` — optional inference-only diagnostic; never configures production / `What are the licensing obligations?`), verified the `docs/2026-08-10-detection-core-port-design.md` link target resolves relative to `ai_engine/`, and re-ran `prettier --write` on the file to match house table formatting (confirmed clean with `prettier --check` afterward). This was found by the lead while spot-reading `ai_engine/README.md` for reconciliation quality, not flagged by either subagent.

## Docs/artifacts auditor findings — full reconciliation

The docs/artifacts auditor independently re-verified everything in the "Required moves," "New documentation scaffolding," "Worktrees," and "Local artifacts" sections above, plus work outside this session's own direct spot-checks:

- **Content-preservation diffing on every moved file** (not just path-matching): confirmed byte-level that nothing was silently dropped across all 41 `be_plan`/`be_audit`/`dev_plan` files, all 8 root-level moved docs, and `LAN_SETUP.md`. The only diffs are the expected internal cross-reference rewrites and the added historical/dated-reference banners.
- **Dangling internal links inside archived plan docs** (`docs/archive/be_plan/00_INDEX.md`, `PROMPTS.md` referencing `18_PKG_scheduled_maintenance.md`, `19_PKG_fe_backend_gaps.md`, etc.): verified these files never existed on `main` at baseline (they're either untracked content specific to the `edge-cases-audit-testing-182a81` worktree, or were never committed at all) — the prior session correctly converted them from misleading clickable links into plain text annotated "historical reference unavailable in this checkout." Accurate, not a defect.
- **Exhaustive link check** (not sampled): a script resolved all ~227 relative Markdown links across every one of the 148 tracked `.md` files in `docs/`, root, `ai_engine/`, `backend/`, `frontend/`, `paper_sync/`. Zero broken links among anything that changed or moved. The only two "broken" categories it initially flagged both trace to content this cleanup never touched and correctly left alone: `ai_engine/adas_transfer/training-configuration.md` (frozen directory, diff-empty) and `backend/content/help/*.md` (117 Help Center article-slug links — confirmed untouched and correctly _not_ rewritten into filesystem links, exactly as the plan requires).
- **Anchor-link check**: both cross-file anchors in the repo (`CONTRIBUTING.md` → `docs/operations/README.md#quickstart--…`, root `README.md` → `docs/operations/README.md#the-dev-panel--…`) manually confirmed to match their target headings' actual GitHub-slug form.
- **Frozen/special-case content**: `ai_engine/adas_transfer/` confirmed completely untouched (empty diff). `ai_engine/docs/training_docs/*.md` external-repo references were converted to honest plain-text citations (`` `path` (source research repository: `../path`; not included here) ``), never silently rewritten to a broken local link. "No Alembic until P9" found only inside properly-labeled `docs/archive/` and one out-of-scope worktree's untracked copy — never presented as current guidance.
- **Backend README accuracy spot-checks** beyond this session's own (password rule `min_length=8, max_length=128` in `app/schemas/user.py`, the audit `CREATE TRIGGER` guard in `app/models/audit.py`/Alembic versions): both confirmed accurate against code.
- **Two real defects found** — `docs/architecture/README.md` mojibake and `ai_engine/README.md`'s broken "Where to look next" table. Both were independently found and fixed by the lead before this report arrived (see "Findings resolved this session" above); the auditor's description of the `ai_engine/README.md` corruption (Module-map rows spliced into the wrong table) matches the lead's diagnosis exactly.
- **Screenshot**: the auditor couldn't view the binary and asked the lead for a visual glance. Done — `frontend/public/help/dashboard-kpis.png` shows only synthetic/seeded demo data (generic camera names matching the seed-profile roster, placeholder KPI numbers), no real footage, credentials, or identifiable information. Confirmed safe.
- **Worktree contents** — corrected this ledger's initial (too-shallow) assessment: `be-p7-backup-ops-8e40ae`, `be-p8-help-center-cc4188`, and `demo-seeding-dev-tools-8a9df5` still hold full ~260–270 MB checkouts (`node_modules/`, lockfiles, a `var/` with test backups/exports), not just the stray `AGENTS.md` this session's first pass diffed. Git-deregistration is still confirmed clean for all three; the physical content is addressed in the "Worktrees" section above with the corrected guidance.
- **`var/`/`tmp/` inventory** — the auditor's full byte/file-count breakdown is folded into the "Local artifacts" table above. Nothing it found changes any disposition already recorded; it added the `gpu-resident/`/`exports/`/`pytest-*` sizing this session's own pass hadn't quantified.

No disagreement between the lead's own review and either subagent's findings on any item; every discrepancy above is a case of the docs auditor's independent pass catching something the lead's first-pass scope had missed (the worktree content depth) or corroborating a defect the lead had already found and fixed (the two documentation-corruption bugs).

## Second-pass independent re-audit (two more read-only agents, plus the lead)

After the first round above, the user asked for another full pass to double-check nothing had been missed. Two fresh read-only agents were spawned with explicit instructions to treat this ledger's own claims as hypotheses to re-verify, not as fact — a third and fourth set of eyes, on top of the original two auditors.

**Both independently verdicted the ledger's account holds up — no blocking issues.** What they found instead were accuracy gaps _in the ledger's own narration_, all now fixed:

- The "Root docs with link-path updates only" bucket wrongly lumped in `ai_engine/README.md`, `backend/README.md`, and `backend/scripts/README.md` as pure path substitutions. They weren't — they carry real (independently verified accurate) content rewrites. Corrected above, in the "Modified files" section.
- This ledger never documented reviewing the plan's explicitly-required Alembic/Playwright/MediaMTX/systemd coverage, even though nothing in those areas needed changing. Now on record in the verification table above.
- The worktree-content description ("full old checkouts") overstated what's left in the three orphaned directories — git had already deleted the entire tracked source tree; only `node_modules/`, `var/`, and a few superseded loose config files remain. Corrected above, in the "Worktrees" section — and it makes the deletion recommendation _more_ confidently safe, not less.
- Two harmless items were missing from this ledger's inventory entirely: empty stray `publisher.log`/`remux.log` at repo root, and an empty top-level `.worktrees/` directory. Found, and deleted this session (see the local-artifacts table above).

Both agents also independently re-ran their own repo-wide mojibake/broken-table sweeps (not trusting "no other occurrences found" from the first pass) and found nothing new, re-diffed 8+ moved files directly against baseline content and found nothing dropped, re-resolved every link/anchor in the 11 highest-traffic docs by hand and found nothing broken, and reconfirmed the TensorRT pin, the AI-outbox boundary, the batch-32 deletion, and the archive "historical" framing are all exactly as claimed. The lead also re-ran the full frontend and backend test suites fresh during this pass — identical results to the first run (353/353 frontend tests, 1214 passed/1 skipped backend), confirming nothing had drifted.

## Local-artifact retention — resolved live with the user

The first pass deferred everything ambiguous under `var/` rather than guess, per the plan's "protect pending review" instruction. During this double-check pass the user was actively available, so those specific calls were put to them directly instead of staying deferred indefinitely:

- **GPU/perf diagnostic scratch** (`var/log/gpu-resident/` + five sibling dirs, ~652 MB) — **user said keep.** Untouched.
- **Generated export artifacts** (`var/exports/`, was 328 MB / 1,860 files) — **user said delete.** Deleted, directory recreated empty (the backend expects `EXPORT_DIR` to exist).
- **Routine dev-session logs** (`var/log/*.log`, `*.transcript.log`, ~130 MB, Aug 16 → today) — **user said keep all of it.** Untouched.
- Trivial pytest/uv-cache scratch (`var/pytest-p27*`, `var/uv-cache-p27/`) and every pure tool cache in the repo (`.ruff_cache/`, `.uv-cache/`, `.codex-uv-cache/`, `.codex-pytest-tmp/`, `backend/.pytest_cache/`, `frontend/node_modules/.vite/`) — deleted without a separate ask, since none of these carry any possible evidence or config value; `ruff check` was re-run immediately after clearing its cache to confirm it rebuilds and still passes clean.
- **One new discovery deferred, not resolved**: `var/p4_ai_drill.db-shm`/`.db-wal` — orphaned SQLite WAL/SHM sidecars with no matching `.db` file. This matches the plan's explicit "Databases and SQLite sidecars" protected category by name, so it's left for the human rather than assumed safe.

## Deferred items requiring a human decision

1. **Four orphaned worktree directories**, now confirmed to hold no tracked source (~230–270 MB combined, almost entirely `node_modules/`): `be-p7-backup-ops-8e40ae`, `be-p8-help-center-cc4188`, `demo-seeding-dev-tools-8a9df5`, `p4-ci-fix`. Git-deregistered but not physically deleted — blocked by Windows path length and this session's sandbox restrictions on `.claude/worktrees/` specifically (a plain `rmdir` worked fine everywhere else this session, including the top-level `be_plan/`/`be_audit/`/`dev_plan/` shells). Commands are in the "Worktrees" section above.
2. **Orphaned SQLite sidecars** `var/p4_ai_drill.db-shm`/`.db-wal` (~757 KB) — matches the plan's protected-category language literally; needs a human call rather than an assumption.
3. **`var/log/gpu-resident/` and sibling diagnostic dirs (~652 MB) and all of `var/log/*.log` (~130 MB)** — deliberately kept per the user's explicit answers above, not overlooked.
4. External `.codex/worktrees/*` (`c127`, `e01d`) — outside this repository's directory, left entirely untouched per the plan's permission-boundary rule. Not this repo's concern to remove.

## Final status

Every tracked file at baseline is accounted for: moved (41+ archive files, 6 validation/handoff docs, `LAN_SETUP.md`), deleted (scratch scripts, delivery-backlog feature, nested lockfile — all with evidence above), or retained with at most a comment/path-only edit (verified line-by-line by the code auditor for every touched runtime/test file, and independently re-verified by a second code auditor in the double-check pass). Seven new documentation files were added per the target layout. Two content-corruption defects, one stale-comment-loss regression in `.gitignore`, one placeholder-metadata gap, and several ledger self-description inaccuracies were found across two full audit rounds (four independent read-only subagents total) and fixed. All local clutter this session could find — regenerable build/test output, every tool cache, empty directory shells, stray zero-byte logs, and the user's explicit retention calls on the ambiguous `var/` content — has been cleaned up; what remains is deliberately kept (GPU diagnostics, dev-session logs) or genuinely needs a human decision (the orphaned worktree checkouts, the orphaned SQLite sidecar). Full verification suite (frontend tests/lint/typecheck/build, full backend pytest, ruff check/format, prettier check) passes clean, re-run twice with identical results. No push, PR, deploy, database reseed, or live Google Drive write occurred at any point.

## Note on a concurrent session

Partway through the double-check pass, files this session never touched started appearing on disk — new `ai_engine/tests/README.md`, `backend/content/README.md`, `backend/tests/README.md`, `e2e/README.md`, `scripts/README.md`, plus edits to `frontend/eslint.config.js`, `frontend/src/components/ui/Button.tsx`, `frontend/src/index.css`, `frontend/VISUAL_CHECKLIST.md`, and `playwright.config.ts`, including a further edit to a `.gitignore` comment this session had itself just written. This session paused and asked before touching anything else; the user confirmed a second, separate session was concurrently running against the same working directory (not an isolated worktree), enriching component READMEs.

Reviewed what landed: it's the same class of work this cleanup has been doing all session — every changed code/config file is a comment-only fix replacing stale references to an untracked, never-committed `FE_Implementation.md` (confirmed absent from `git ls-tree 041c863`) with pointers to the real tracked docs (`frontend/README.md`, `frontend/VISUAL_CHECKLIST.md`, `e2e/README.md`, `index.css`). The five new component READMEs were checked for the same defect patterns hunted all session (mojibake sweep: clean; every relative link and anchor in all five resolves, verified by hand including the GitHub-slug math on `docs/operations/README.md#the-fast-path-scriptsstart-devps1`). The full verification suite was re-run once more against the combined tree — frontend lint clean, 353/353 tests, `ruff check`/`ruff format --check` clean, `prettier --check .` clean across the whole repo. No conflicts, no overlap in files edited by both sessions in a way that fought each other; the other session's `.gitignore` edit was a strict improvement on this session's own comment, not a revert.

This ledger's scope remains this session's own work; the other session's component-README additions are noted here for continuity but weren't independently re-audited beyond the coherence checks above — that's the other session's own record to keep, if it produces one.
